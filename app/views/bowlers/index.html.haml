%h1 Listing bowlers

%table
  %thead
    %tr
      %th Name
      %th Usbc number
      %th
      %th
      %th

  %tbody
    - @bowlers.each do |bowler|
      %tr
        %td= bowler.name
        %td= bowler.usbc_number
        %td= link_to 'Show', bowler
        %td= link_to 'Edit', edit_bowler_path(bowler)
        %td= link_to 'Destroy', bowler, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Bowler', new_bowler_path
